---
const {date} = Astro.props;
const dateObject:Date = new Date(date);
const daysOfWeek:any = {
    0: "Sunday",
    1: "Monday",
    2: "Tuesday",
    3: "Wednesday",
    4: "Thursday",
    5: "Friday",
    6: "Saturday"
}
---
{`${daysOfWeek[dateObject.getDay()]} ${date}`}